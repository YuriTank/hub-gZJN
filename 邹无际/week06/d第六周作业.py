"""

在 BERT 文本分类模型的微调中，不同的训练策略对最终性能有着明显且多维度的影响。
池化策略决定了句子向量的提取方式。CLS 池化作为基准方法，能够有效捕捉语义；均值池化对所有 Token 向量取平均，能平滑局部噪声，在短文本分类中表现出较强的鲁棒性；最大值池化提取每个维度上的最大激活值，专注捕捉序列中最显著的特征词。在处理由个别关键词主导分类的任务时，最大值池化能产生更敏感的响应，但也更容易受到高频噪声词的干扰。

针对数据集中存在的类别不均衡问题，引入加权损失函数（Weighted Loss）很重要。该方法通过增加少数类样本的惩罚权重，提升了模型对低频类别的召回率（Recall），在宏观 F1 分数（Macro F1）上取得更优的综合表现，避免了模型向多数类倾斜的现象。
采用分层学习率（Layer-wise Learning Rate）策略，对预训练的 BERT 层使用较小的学习率保护其语言知识，对随机初始化的分类头使用较大的学习率加速收敛，能够提升训练的稳定性和效率。

对比不同池化方式的特征提取偏好，组合“加权损失函数”与精细的“分层学习率”控制，可以克服数据分布不均的问题，模型在各类别上的识别能力更加均衡，达到更好的分类效果。
"""
